{
  "compilerOptions": {
    "baseUrl": "./",       // This must be set if "paths" is set
    "paths": {
      "@/*": ["src/*"]     // Maps all imports starting with "@" to the "src" directory
    }
  },
  "exclude": ["node_modules"]
}
